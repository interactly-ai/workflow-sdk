# Phase 0 baseline — 2026-08-06

Recorded on branch `shiva_phase_1` before any sync work, per Phase 0 of the update plan.

**Headline: the baseline was fictional.** Three of the four quality gates had never actually run.
`pip install -e .` failed at metadata generation, so `make install` had been broken since 2026-07-14;
`make lint` had never passed; and `make typecheck` aborted on a configuration error before checking a
single file. Only `make test` was genuinely green. Each is detailed below with what was done about it.

---

## Environment

| Item | Value |
|---|---|
| Python | 3.13.13 (`.venv`, created by `make install`) |
| Platform | darwin 25.5.0 |
| Target server | `https://api-dev.interactly.ai/workflows` (dev) |
| Credential | opaque 36-char API key, live-verified `200` |
| Branch | `shiva_phase_1` |

> **Note on the pre-existing `venv/` directory.** The repo also contains a `venv/` (as opposed to the
> `.venv/` the Makefile uses). Every `make` target targets `.venv`. If your editor reports missing
> packages such as `pydantic-settings` or `websockets`, it has selected `venv/` — point it at `.venv/`.

---

## Gate 1 — `make install` ❌ → ✅ FIXED (was blocking everything)

```
OSError: License file does not exist: LICENSE
error: metadata-generation-failed
```

Commit `7d26b4b` ("Remove license", 2026-07-14) deleted the root `LICENSE` file but left
`license = { file = "LICENSE" }` in `pyproject.toml`. Hatchling validates that path at metadata
generation, so **`pip install -e .` — and therefore the documented `make install` — has been broken
for three weeks.** `configs/pyproject.toml` was unaffected (`configs/LICENSE` still exists).

**Fix:** dropped the `license` key and the now-inaccurate `License :: OSI Approved :: MIT License`
classifier from the root `pyproject.toml`, with a comment recording why. Install now succeeds.

---

## Gate 2 — `make test` ✅ GREEN (the one honest gate)

```
364 passed in 1.99s
```

Unchanged at **364 passed** after all Phase 0 edits. This is the regression anchor.

---

## Gate 3 — `make lint` ❌ 1117 errors → ✅ GREEN

`ruff check src/ tests/` reported **1117 errors**. The breakdown mattered more than the number:

| Rule | Count | Nature |
|---|---|---|
| `UP045` non-pep604-annotation-optional | 544 | style |
| `UP006` non-pep585-annotation | 328 | style |
| `UP035` deprecated-import | 81 | style |
| `UP037` quoted-annotation | 66 | style |
| `UP007` non-pep604-annotation-union | 2 | style |
| **`UP` subtotal** | **1021** | **style only** |
| `F401` unused-import | 50 | real |
| `I001` unsorted-imports | 28 | real |
| `N814` camelcase-imported-as-constant | 11 | intentional pattern |
| `F841` unused-variable | 3 | real |
| `E402` module-import-not-at-top | 1 | real |
| `F821` **undefined-name** | 1 | **real latent bug** |

### Decision: `UP` (pyupgrade) removed from the selected ruleset — please review

91% of the findings were pyupgrade rules that would rewrite `Optional[X]` → `X | None` and
`List[X]` → `list[X]`. **Enforcing them would actively sabotage the sync strategy.**
`configs/src/interactly_configs/` is a deliberate mirror of `agentic_workflow_framework/configs/`,
which uses the typing-module style — the two currently agree *exactly* (both have 43 `Optional[`
occurrences in `llm.py`). Auto-upgrading would permanently diverge the mirror from upstream and turn
every future sync from a diff into a manual reconciliation, which is precisely what plan principle #2
exists to prevent. `src/interactly/` is kept in the same style so both halves read alike.

`make lint` only covers `src/ tests/` today, so `configs/` was never at risk from the Makefile — but a
plain `ruff check .` would have been enough to do the damage. The ruleset now says so explicitly.

`N814` was also globally ignored: all 11 sites are the same intentional pattern — `interactly.types.*`
models importing config classes lazily inside functions and aliasing them short (`EdgeConfig as _EC`)
to keep `interactly_configs` optional and avoid import cycles. One documented ignore beats 11 identical
`noqa`s.

### Real fixes applied

- **`F821` — genuine latent bug.** `_utils/_retry.py:145` annotated `with_retry_async`'s `send`
  parameter as `"asyncio.Coroutine[...]"` while `import asyncio` sat *inside the function body*. The
  quoted annotation plus `from __future__ import annotations` meant it never raised at runtime, but the
  annotation was unresolvable for `typing.get_type_hints`, mypy, and any doc tooling. Moved the import
  to module level and removed the redundant local one.
- `F401` ×49 — genuinely unused imports, auto-fixed. Verified first that **none** were in an
  `__init__.py`, so no re-export was broken.
- `F401` ×1 — `configs.py:214` is an intentional optional-dependency availability probe inside
  `try/except ImportError` that appends to `__all__`. Given a `# noqa: F401` and an explanatory comment.
- `I001` ×28, `W293` ×2 — auto-fixed.
- `E402` — `_types.py` imported `TypeVar` in the middle of the file for no reason; folded into the
  top-level `typing` import.
- `F841` ×3 — unused `route = respx.post(...)` locals in two test files; assignment dropped.

**Result: `All checks passed!`, with `make test` still at 364 passed.**

---

## Gate 4 — `make typecheck` ❌ config error → ❌ 79 errors → ✅ **GREEN**

> **Updated 2026-08-07.** All 79 were fixed on request. Final state:
> `Success: no issues found in 98 source files`. The original analysis is kept below because the
> root causes are worth recording; the fixes are summarised at the end of this section.


Originally mypy aborted before checking anything:

```
src/interactly/types/edges/edge.py: error: Source file found twice under different module names:
"src.interactly.types.edges.edge" and "interactly.types.edges.edge"
Found 1 error in 1 file (errors prevented further checking)
```

Cause: stray empty `src/__init__.py` and `configs/src/__init__.py` made `src` look like a package, so
every module resolved under two names. Both files were empty, referenced nowhere, and irrelevant to
packaging (hatch builds `src/interactly` and `src/interactly_configs` directly). **Removed.**

With the blocker gone mypy runs for the first time and reports **79 errors in 19 files** under
`strict = true`:

| Error code | Count | Nature |
|---|---|---|
| `type-arg` (bare `dict`/`Dict`) | 26 | mechanical |
| `no-any-return` | 23 | mechanical |
| `unused-ignore` | 17 | mechanical |
| `union-attr` | 6 | needs None-guards |
| `call-arg` | 2 | substantive |
| `arg-type` | 2 | substantive (`WorkflowHandle` client types) |
| `return-value`, `name-defined`, `attr-defined` | 3 | substantive |

Worst-affected: `runtime/handle.py` (15), `resources/tools/tools.py` (12), `_streaming.py` (9).

### How the 79 were fixed (2026-08-07)

Two root causes accounted for most of them, and fixing those was worth more than the error count suggests.

**1. `is_given()` did not narrow types.** It was declared `-> bool`, so no checker could narrow
`NotGivenOr[T]` to `T` inside `if is_given(x):`. Every such branch was an error, which is what had
pushed call sites into redundant `is not None` guards. Redeclaring it as `TypeGuard[T]` fixed **five
errors across two files at once** — and SDK users get the same narrowing in their editors.

**2. A broken annotation was laundering types into `Any`.** `_retry.py` declared
`send: Callable[[], "asyncio.Coroutine[None, None, httpx.Response]"]`. `asyncio.Coroutine` does not
exist, so the annotation was unresolvable, the parameter degraded to `Any`, and both return statements
in the function inherited it. One correct `Coroutine[Any, Any, httpx.Response]` fixed three errors.

Also found and fixed while working through the list:

- **`webhooks.list_events` returned an un-awaited coroutine from its `_next_page` callback.** Declared
  `-> AsyncPage[WebhookEvent]`, actually returned a coroutine. It works today only because
  `AsyncPage.next_page` awaits whatever it is handed — but the annotation was a lie and any direct
  caller would have received a coroutine. Now `async def` + `await`, matching every other async pager.
- **`AsyncPage.fetch_page` was typed `Callable[[int], Any]`** with a comment explaining what it really
  was. Now `Callable[[int], Awaitable[AsyncPage[ItemT]]]`, so the comment is unnecessary.
- **`_apply_hydrated_name` called `.get()` on a possibly-`None` value.** `isinstance(body.get(x), dict)`
  narrows that call's result, not the *separate* call on the other branch of the ternary. Bound once,
  then narrowed.

The remainder were mechanical: bare `dict`/`Dict` annotations given parameters (26), `Any`-returning
`raw.get(...)` expressions wrapped in `cast` (23), and stale or mis-coded `type: ignore` comments
removed or corrected (17). Two `cast`s were added where a resource passes `self._client` to a handle:
the attribute is declared as the transport base `SyncAPIClient`, but a resource is only ever built from
the public `WorkflowClient`, which is what the handle needs — narrowing there was correct, whereas
widening the handle's parameter would have typed away the `.runs` attribute it uses.

`make test` stayed at **364 passed** throughout, and notebooks `01`, `03`, `12` pass against dev after
the changes.

---

## Security finding — `.env` was tracked in git ✅ FIXED

`workflow-sdk/.env`, containing `INTERACTLY_API_KEY`, was **tracked and committed** (commits `a266b5d`,
`af22255`). `.gitignore` covered `.envrc`, `.venv`, `env/`, `venv/` — but not `.env`.

**Severity is low, and verified so rather than assumed:** the committed value is the *old* 288-char JWT,
and it returns **401** against dev today. No live credential is exposed in git history.

It was still a live leak *vector*, and Phase 0 was about to write the current working key to exactly
that path. Fixed before doing so:

1. `.env`, `.env_dev`, `.env_qa` added to `.gitignore` with a comment naming the two prior commits.
2. `git rm --cached .env` — untracked, file preserved on disk.
3. Added a committed **`.env.example`** with the four required keys and empty values.
4. Copied the working `../.env_dev` into `.env` and verified `200` against dev.

**No history rewrite performed and no credential rotated** — both are your call, and neither looks
warranted given the committed key is already dead. Flagging so the decision is yours, not mine.

---

## Notebook harness ✅ GREEN

```
Running 2 notebook(s) against https://api-dev.interactly.ai/workflows
  PASS  01_quickstart.ipynb          (2.8s)
  PASS  03_workflow_versions.ipynb   (3.5s)
SUMMARY: 2/2 passed
```

`_run_e2e.py --list` enumerates **41** notebooks (18 core + 23 examples). Both spot-checks pass against
dev, so the harness is a usable regression gate for later phases.

---

## Pre-existing working-tree noise (not mine)

Three notebooks were already modified in the working tree before this work began:
`wf_example_notebooks/wf_example_progression_{1,2,3}.ipynb` — execution-count and output churn from an
earlier local run. Left untouched and uncommitted; all Phase 0/1 commits stage explicit paths only.

---

## Summary

| Gate | Before | After |
|---|---|---|
| `make install` | ❌ broken since 2026-07-14 | ✅ works |
| `make test` | ✅ 364 passed | ✅ 364 passed |
| `make lint` | ❌ 1117 errors | ✅ All checks passed |
| `make typecheck` | ❌ aborted on config error | ✅ **Success: no issues found in 98 source files** |
| `.env` in git | ❌ tracked (dead key) | ✅ untracked + ignored + `.env.example` |
| notebook e2e | untested | ✅ pass |
| `make refs-check` | did not exist | ✅ 681 source units, all references resolve |
